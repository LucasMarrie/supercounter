{
  "name": "supercounter",
  "version": "1.0.0",
  "private": true,
  "description": "A global counter that increments on the server and broadcasts to every connected client in real time.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
